// Package config loads builder configuration: shell env (via env package),
// tracked TOML policy (tools/builder.toml), per-checkout user state
// (tools/builder.local.toml), and per-command metadata (ps/cmd/build.toml).
package config
